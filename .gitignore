node_modules/
dist/
coverage/
playwright-report/
test-results/
.data/
.env
.DS_Store
*.tsbuildinfo
# Large local-only source material (rulebook PDF, map scans) — kept on disk, never committed
source-material/

# Terrain generation scratch output (gen:map / gen:map-control default dir)
/terrain/

# Visual brainstorming companion
.superpowers/

# Local screenshot helper (untracked dev artifact)
.shot.mjs
